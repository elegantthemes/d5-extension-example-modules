// External Dependencies.
import React, { ReactElement } from 'react';

/**
 * Functional component that is used by wp.blocks.serialize() to parse column on block array model
 * into serialized GB format.
 *
 * @returns {ReactElement}
 */
const ChildModuleSave = (): ReactElement => (
  <React.Fragment>
    {/* TODO Adjust save component based on result of discussion of FE rendering */}
  </React.Fragment>
);

export {
  ChildModuleSave,
};
