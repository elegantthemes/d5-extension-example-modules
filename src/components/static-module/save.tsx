// External Dependencies.
import React, { ReactElement } from 'react';

/**
 * Functional component that is used by wp.blocks.serialize() to parse column on block array model
 * into serialized GB format.
 *
 * @returns {ReactElement}
 */
const StaticModuleSave = (): ReactElement => (
  <React.Fragment>
  </React.Fragment>
);

export {
  StaticModuleSave,
};
